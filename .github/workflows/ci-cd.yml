name: FlertAI Build Status

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  status-check:
    name: ✅ Status Check
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4
      
    - name: ✅ Success
      run: |
        echo "✅ Repositório OK"
        echo "📦 Build manual: flutter build web"
        echo "🚀 Deploy: Netlify Drag & Drop"
